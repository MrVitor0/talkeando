# Copie para music-bot.env somente em deploys manuais. No deploy automatizado,
# o GitHub Actions gera esse arquivo a partir dos secrets do environment production.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# Optional. Public playlists, tracks and albums work with client credentials
# alone. Only set this (via `node music-bot/scripts/spotify-authorize.js`, as a
# secret) if you need PRIVATE / collaborative playlists.
SPOTIFY_REFRESH_TOKEN=
# Discovery only: resolves YouTube links/playlists to title + artist. Playback
# comes from SoundCloud/Audius. No cookies needed.
YOUTUBE_API_KEY=
# Opcional. O bot tenta o acesso público legado quando estiver vazio.
AUDIUS_API_KEY=
# Music is normalized by default; lower this if the community still finds it
# louder than voice (for example 0.10). Use 1 only for an unattenuated source.
MUSIC_VOLUME=0.15
MUSIC_LOUDNORM=1
