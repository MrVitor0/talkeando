# Schemas de protocolo

Estes schemas validam o envelope comum. O shape específico de cada operação é
normativo em `SDD/contracts/`; cada novo op precisa receber um schema específico
antes de o cliente e o servidor o consumirem em produção.
